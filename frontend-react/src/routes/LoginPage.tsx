// TODO(agente auth-shell): pantalla de login real (email/password contra
// Supabase Auth via react-hook-form + zod). Placeholder temporal para que
// el build/routing completo funcione mientras se desarrolla en paralelo.
export function LoginPage() {
  return (
    <div className="flex h-dvh items-center justify-center bg-background text-texto">
      <p className="text-sm text-texto-secundario">Login (pendiente)</p>
    </div>
  );
}
